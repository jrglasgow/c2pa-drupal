<?php

/**
 * @file
 * Primary module hooks for c2pa module.
 */

/**
 * Implements template_preprocess_filter_caption().
 */
function c2pa_preprocess_filter_caption(&$vars) {
  $vars['classes'] .= ' c2pa-wrapper';
  $vars['#attaches']['library'][] = 'c2pa/c2pa';
}
